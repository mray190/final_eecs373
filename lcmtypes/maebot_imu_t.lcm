struct maebot_imu_t
{
    int64_t utime;      // variscite

    double acceleration[3];        // x, y, z
    double angular_velocity[3];    // x, y, z  -- around z-axis is dTheta/dt
    double orientation[3];         // x, y, z  // around z-axis is theta
}
